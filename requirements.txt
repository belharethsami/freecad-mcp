# FreeCAD MCP Server dependencies
# Install into FreeCAD's Python environment:
# /path/to/FreeCAD/python -m pip install -r requirements.txt

mcp>=1.0.0

# For split-screen dual document view (side-by-side image merging)
Pillow>=9.0.0

